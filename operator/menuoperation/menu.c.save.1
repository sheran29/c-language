#include<stdio.h>
void add(int x[3][3],int y[3][3],int sum[3][3]);
void display(int result[3][3]);
 int main()
 {
     int i,j,l,k,n,v;
     int a[10][10],b[10][10],c[10][10],sum[10][10];
 //    printf("how many row:");
 //    scanf("%d",&l);
//     printf("how many columns:");
 //    scanf("%d",&k);
     printf("enter 1st matrix:");
     for(i=0;i<3;i++)
     {
       for (j=0;j<3;j++)
       {
           scanf("%d",&a[i][j]);
        }
     }
    printf("enter 2nd matrix:");
    for(i=0;i<3;i++)
    {
      for(j=0;j<3;j++)
       {
        scanf("%d",&b[i][j]);
       }
     }
   printf("enter your choice");
   printf("1.Addition\n2.subtraction\n3.mulitiplication,\n4.exist");
   scanf("%d",&n);
  {
  switch(n)
  case 1:add(a,b,sum);
         display(sum);
         break;
  //case 2:sub(x[l][k], y[l][k]);
   //      printf("subtraction of matrix:%d",c[l][k]);
     //    break;
 // case 3:mul( x[l][k], y[l][k] );
 //       printf("multiplication of matrix:%d", c[l][k]);
   //      break;
  default :printf("enter wrong choice");
           break;
   }
  return 0;
}
 //int sub(int x[l][k], int y[l][k]);
 // { 
   // for(int i=0;i<l;i++)
     // { 
       // for(int j=0;j<k;j++)
        //{
        // c[i][j]=x[i][j]-y[i][j];
        // }
      // }
//   return 0;
//  }
// int mul(int x[l][k], int y[l][k] );
 // {
  //  for(int i=0;i<l;i++)
    //  {
      //  for(int j=0;j<k;J++)
       // {
         // c[l][k]=0;
           //for(int v=0;v<k;v++)
           // c[i][j]+=o[i][j]*p[v][i];
        // }
    //  }
   //  return 0;
 //  }

void add(int x[3][3], int y[3][3], int sum[3][3])
   {
     for(int i=0;i<3;i++)
      {
        for(int j=0;j<3;j++)
        sum[i][j]=x[i][j]+y[i][j];
       }
 
    }
void display(int  result[3][3])
{
 
  for(int i=0;i<3;i++)
      {for(int j=0;j<3;j++)
         printf("%d\t",result[i][j]);
        printf("\n");
      }
}
     
